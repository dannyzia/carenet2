/**
 * Sandbox / “Experience the App” — single source for UX copy and offline draft TTL.
 * `src/backend/offline/db.ts` uses the same value for form draft expiry.
 */
export const EXPERIENCE_LOCAL_DRAFT_RETENTION_DAYS = 7;
