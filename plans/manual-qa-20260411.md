# Manual QA log — CareNet 2

**Date:** 2026-04-11  
**App base URL (local):** `http://localhost:5173/`  
**Credentials:** see `plans/00 Test Credentials.md` (shared password documented there).

**Agent environment note:** The automated agent cannot reach your machine’s `localhost` from every shell context. Run the checklists in your browser at `http://localhost:5173/`, and use **Playwright** (below) for automated smoke when `VITE_PLAYWRIGHT_E2E` is enabled.

---

## Automated E2E (Playwright)

- **Command:** `npm run test:e2e:phase0` (or `npx playwright test e2e/carenet/ --project=chromium`).
- **If port 5173 is busy** (normal `npm run dev`): set **`PW_TEST_PORT`** to a free port (e.g. `5174`–`5178`). `playwright.config.ts` starts Vite with `VITE_PLAYWRIGHT_E2E=true` on that port. PowerShell: `$env:PW_TEST_PORT="5175"; npx playwright test e2e/carenet/ --project=chromium`.
- **Scope:** Demo `@carenet.demo` flows + route smoke — **not** a substitute for the full manual matrix with `*@indigobangladesh.xyz` accounts.

---

## Summary (counts)

| Category | Count | Notes |
|----------|------:|-------|
| **P0** (ship blocker: crash/data loss/security) | 0 | **Fixed in repo:** `AdminDashboardPage` referenced undefined `urgent` (runtime crash on success path). |
| **P1** (major UX / wrong nav / broken primary path) | 0 | **Fixed:** BottomNav Search for non–care-seeker roles now uses `/global-search`. **Fixed:** email login with `needsMfa` navigates to `/auth/mfa-verify`; routes `auth/mfa-verify` and `auth/mfa-setup` registered. |
| **P2** | 0 | — |
| **P3** (cleanup / duplicate config) | 0 | **Fixed:** removed duplicate `guardian/patient/:id` route entry. |
| **Blocked by env** (not scored as app bug) | varies | MFA, missing Supabase keys, RLS, Tolgee, etc. — log per row when observed. |

---

## Part 0 — Account / role matrix (fill on login)

Confirm **active role** after login (and at `/auth/role-selection` if multi-role). The repo does **not** map `agent@` / `agent1@` to a role in code; roles come from your Supabase session / metadata.

| Login email | Intended test role | Actual role(s) seen | Agency vs admin for agents? |
|-------------|-------------------|---------------------|-----------------------------|
| guardian1@indigobangladesh.xyz | Guardian | *(fill)* | — |
| guardian2@indigobangladesh.xyz | Guardian | *(fill)* | — |
| patient1@indigobangladesh.xyz | Patient | *(fill)* | — |
| patient2@indigobangladesh.xyz | Patient | *(fill)* | — |
| caregiver1@indigobangladesh.xyz | Caregiver | *(fill)* | — |
| caregiver2@indigobangladesh.xyz | Caregiver | *(fill)* | — |
| agent@indigobangladesh.xyz | Agency (default assumption) | *(fill)* | If **admin**, run admin smoke only and document “no agency test user in credentials file” or obtain an agency login. |
| agent1@indigobangladesh.xyz | Agency (default assumption) | *(fill)* | Same as above. |
| shopowner1@indigobangladesh.xyz | Shop | *(fill)* | — |
| shopowner2@indigobangladesh.xyz | Shop | *(fill)* | — |

**Mock-auth caveat (local dev without Supabase URL/key):** `mockLogin` accepts any non-`@carenet.demo` email with password length ≥ 8 and returns a synthetic user with **`roles: []`** and **`activeRole: "guardian"`** (`src/frontend/auth/mockAuth.ts`). That does **not** mirror production roles for `*@indigobangladesh.xyz`. Use real Supabase-backed sessions for credential-file accounts.

---

## Part A — Issue log (living)

Use one row per broken/dead/blocked control. Severity: **P0** / **P1** / **P2** / **P3** / **Blocked-env**.

| Account | Role | Page/URL | Control (link/button) | Expected | Actual | Severity | Screenshot |
|---------|------|-----------|------------------------|----------|--------|----------|------------|
| *(resolved)* | — | — | Prior static findings (BottomNav dead search, duplicate route, admin `urgent` crash, missing MFA routes) | — | Addressed in codebase (see Summary). | — | — |
| | | | | | *(add rows during your browser pass)* | | |

**Duplicate route note:** Duplicate `guardian/patient/:id` was removed; log here only if patient detail ever misbehaves in QA.

---

## Part A — Sidebar + shell checklist (from `AuthenticatedLayout.tsx`)

For each login, after selecting the correct role: tick every **sidebar primary/secondary** link, **collapsible** sections (Browse / Support / App), **header** notifications, **/settings**, and **BottomNav** (Home, Back, Search, Messages, Menu). Then hit **shared** routes where visible: `/billing`, `/wallet?role=…`, `/contracts?role=…`, `/messages`, `/notifications`, `/dashboard` (if linked).

### Guardian (`guardian1`, `guardian2`)

- [ ] `/guardian/dashboard`
- [ ] `/guardian/care-requirements`
- [ ] `/guardian/marketplace-hub`
- [ ] `/guardian/placements`
- [ ] `/guardian/patients` → open a patient → `/guardian/patient/:id`
- [ ] `/guardian/schedule`
- [ ] `/guardian/messages`
- [ ] `/guardian/profile`
- [ ] `/guardian/payments`
- [ ] `/billing`
- [ ] `/wallet?role=guardian`
- [ ] `/contracts?role=guardian`
- [ ] `/guardian/reviews`
- [ ] `/guardian/search` *(hidden if `VITE_CARE_SEEKER_CAREGIVER_CONTACT_ENABLED` false — then marketplace tab substitute)*
- [ ] `/guardian/caregiver-comparison` *(same flag)*
- [ ] `/guardian/booking`
- [ ] `/guardian/care-requirement-wizard`
- [ ] `/guardian/patient-intake`
- [ ] `/guardian/family-hub`
- [ ] Support: `/support/help`, `/support/ticket`, `/support/refund`
- [ ] `/settings`, `/notifications`, BottomNav tabs

### Patient (`patient1`, `patient2`)

- [ ] `/patient/dashboard` through profile + tools + health + privacy sections (mirror `patient` entries in `AuthenticatedLayout.tsx`)
- [ ] Shared: `/billing`, `/wallet?role=patient`, `/contracts?role=patient`, `/settings`, `/notifications`, BottomNav

### Caregiver (`caregiver1`, `caregiver2`)

- [ ] All `caregiver/*` sidebar paths (main, patient care, finance, growth)
- [ ] **BottomNav → Search** → expect issue unless fixed (see issue table)
- [ ] `/caregiver/profile`, `/caregiver/documents`, `/caregiver/portfolio` (one holistic profile journey for Part B)
- [ ] Shared: `/billing`, `/wallet?role=caregiver`, `/contracts?role=caregiver`, `/settings`, `/notifications`

### Agency (`agent`, `agent1` — only if role = agency)

- [ ] All `agency/*` sidebar paths (main, operations, finance/settings)
- [ ] **BottomNav → Search** → expect issue (see issue table)
- [ ] `/agency/package-create`, `/agency/care-packages`, `/agency/document-verification`
- [ ] Shared: `/billing`, `/wallet?role=agency`, `/contracts?role=agency`, `/settings`

### Shop (`shopowner1`, `shopowner2`)

- [ ] `/shop/dashboard`, `/shop/products`, `/shop/orders`, `/shop/inventory`, `/shop/analytics`, `/billing`
- [ ] Management: `/shop/product-editor`, `/shop/fulfillment`, `/shop/merchant-analytics`, `/shop/onboarding`
- [ ] **BottomNav → Search** → expect issue (see issue table)
- [ ] `/settings`, `/notifications`, BottomNav

### If `agent@` is admin (split log per plan)

- [ ] `/admin/dashboard` … full admin sidebar from `AuthenticatedLayout` (`admin` sections: main, finance, system, contentConfig)
- [ ] Document inability to seed agency packages under this login unless another agency account exists.

---

## Route inventory — authenticated (`src/app/routes.ts`)

Use this as the **authoritative list** of role-prefixed pages when classifying **Dead** vs **Broken**.

**Shared:** `dashboard`, `schedule/daily`, `documents/view/:id`, `settings`, `notifications`, `messages`, `wallet`, `wallet/transfer-history`, `wallet/top-up`, `contracts`, `contracts/:id`, `contracts/disputes`, `contracts/disputes/:id`, `billing`, `billing/invoice/:invoiceId`, `billing/submit-proof/:invoiceId`, `billing/verify/:proofId`, `dev/connectivity`.

**Caregiver:** all `caregiver/...` paths from `routes.ts` (dashboard through `caregiver/alerts`).

**Guardian:** all `guardian/...` paths including `guardian/emergency`, marketplace, booking, care requirements, placements, live tracking, scorecard, family board, incident report, etc.

**Agency:** all `agency/...` paths including `agency/marketplace-browse` (legacy redirect), package flows, jobs, incidents, care-scorecard.

**Patient:** all `patient/...` paths (reuses guardian pages for care requirements / marketplace where configured).

**Admin:** nested under `admin/` + `RequireAdminRoute`.

**Moderator:** `moderator/dashboard` … `moderator/escalations`.

**Shop (merchant):** `shop/dashboard` … `shop/product-editor/:id`.

**Public shop front (customer):** under `ShopFrontLayout` — `shop`, `shop/category/:category`, `shop/product/:id`, cart, checkout, etc. (same `/shop` prefix; specificity resolves merchant vs storefront).

---

## Part B — Seed checklist (manual, in browser)

Use **distinct titles** so rows are easy to find and delete later. After each create, paste **IDs** (from URL or list) into the bullets.

### Guardian — `guardian1@indigobangladesh.xyz`

- [ ] Care requirement **QA G1 Req A** — id: ___
- [ ] Care requirement **QA G1 Req B** — id: ___
- [ ] Patient **QA G1 Patient A** — id: ___  
  Wizard: `/guardian/care-requirement-wizard` · List: `/guardian/care-requirements` · Intake/list: `/guardian/patient-intake`, `/guardian/patients`

### Guardian — `guardian2@indigobangladesh.xyz`

- [ ] Care requirement **QA G2 Req A** — id: ___
- [ ] Care requirement **QA G2 Req B** — id: ___
- [ ] Patient **QA G2 Patient A** — id: ___

### Agency — per **agency-capable** login (each account that has agency role)

**agent@** (only if agency):

- [ ] Package **QA Agt Pkg A** — id: ___
- [ ] Package **QA Agt Pkg B** — id: ___  
  Flow: `/agency/package-create` (×2) · Verify: `/agency/care-packages`

**agent1@** (only if agency):

- [ ] Package **QA Agt1 Pkg A** — id: ___
- [ ] Package **QA Agt1 Pkg B** — id: ___

*(If an agent login is admin only, note here and skip package rows for that login.)*

### Caregiver — holistic profile (same narrative: profile + documents + portfolio)

**caregiver1@indigobangladesh.xyz**

- [ ] Profile saved (fields/bio aligned to story) — notes: ___
- [ ] Credential(s) uploaded `/caregiver/documents` — file names / doc ids: ___
- [ ] Portfolio `/caregiver/portfolio` matches profile — id/slug: ___

**caregiver2@indigobangladesh.xyz**

- [ ] Profile — notes: ___
- [ ] Documents — ids: ___
- [ ] Portfolio — ids: ___

Optional agency verification: `/agency/document-verification` if you run an agency session the same day.

### Shop — four products per owner

**shopowner1@indigobangladesh.xyz** — `/shop/product-editor` (and `/shop/products`)

- [ ] **QA S1 Prod 1** — id: ___
- [ ] **QA S1 Prod 2** — id: ___
- [ ] **QA S1 Prod 3** — id: ___
- [ ] **QA S1 Prod 4** — id: ___

**shopowner2@indigobangladesh.xyz**

- [ ] **QA S2 Prod 1** — id: ___
- [ ] **QA S2 Prod 2** — id: ___
- [ ] **QA S2 Prod 3** — id: ___
- [ ] **QA S2 Prod 4** — id: ___

---

## Part C — Execution order (reminder)

1. Fill **Part 0** matrix (resolve agency vs admin first).  
2. Run **Part A** in order: Guardian → Patient → Caregiver → Agency → Shop (+ admin smoke if needed).  
3. Run **Part B** only on screens that are not dead/blocked; note blockers in the issue table.

---

## Revision history

| When | Who | What |
|------|-----|------|
| 2026-04-11 | Cursor agent | Initial file: static P1/P3, checklists, seed names, localhost handoff note. |
| 2026-04-13 | Cursor agent | Summary refresh: fixes for MFA routes + login `needsMfa`, BottomNav → `/global-search`, admin `urgent`, duplicate route removal; Playwright `PW_TEST_PORT` doc; admin/moderator/shop E2E assertions aligned to operational dashboards. |
